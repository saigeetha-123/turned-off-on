# turned-off-on
This project simulates a simple switch board controlling a bulb and a cat image. When the switch is turned off, the bulb goes dark and the cat's eyes glow. When the switch is turned on, the bulb lights up and the normal cat image is shown. The UI is built using HTML, CSS (with Bootstrap), and JavaScript for interactive behavior
